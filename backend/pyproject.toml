[project]
name = "cote-ivoire-dashboard-backend"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "httpx",
    "groq",
    "mcp",
    "python-dotenv",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
